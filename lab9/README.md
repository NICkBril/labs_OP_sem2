# Lab 9 - Logging Decorator

Task: Create a decorator-based logging system with configurable levels (INFO, DEBUG, ERROR).
Must support:
- Sync and async functions
- Different output types (Console, File)
- Structured logging (JSON, Text)